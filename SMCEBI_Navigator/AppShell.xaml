<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="SMCEBI_Navigator.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:SMCEBI_Navigator.Views">
    
    <TabBar>
        <ShellContent Title="Home"  ContentTemplate="{DataTemplate views:HomePage}" />
        <ShellContent Title="Picker" ContentTemplate="{DataTemplate views:MapPickerPage}" />
        <ShellContent Title="Map" ContentTemplate="{DataTemplate views:MapDisplayPage}" />
    </TabBar>
</Shell>
